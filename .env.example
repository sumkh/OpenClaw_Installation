# Example environment variables for OpenClaw
# Copy to .env (DO NOT commit .env) and fill values

OPENCLAW_ENV=production
OPENCLAW_PORT=8080

# Path inside container (mounted read-only)
GOOGLE_APPLICATION_CREDENTIALS=/opt/openclaw/secrets/google-sa.json
WHATSAPP_CRED_FILE=/opt/openclaw/secrets/whatsapp.conf

# Docker image tag
OPENCLAW_IMAGE=openclaw:local
